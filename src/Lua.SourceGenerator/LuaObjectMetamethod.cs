namespace Lua.SourceGenerator;

// same as Lua.LuaObjectMetamethod

internal enum LuaObjectMetamethod
{
    Add,
    Sub,
    Mul,
    Div,
    Mod,
    Pow,
    Unm,
    Len,
    Eq,
    Lt,
    Le,
    Call,
    Concat,
    Pairs,
    IPairs,
    ToString,
}
